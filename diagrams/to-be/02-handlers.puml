@startuml
title TaskFlow — TO-BE KAS Handler Taxonomy
skinparam componentStyle usecase
left to right direction
' Generated by /arch-diagrams from /home/dmitriy/Projects/TaskFlow
' DO NOT EDIT BY HAND — re-run /arch-diagrams to regenerate

package "invoke handlers" #LightBlue {
  (login) <<invoke>>
  (register) <<invoke>>
  (check_token) <<invoke>>
  (crud_execute) <<invoke>>
  (get_tasks_by_month) <<invoke>>
  (batch_soft_delete_cascade) <<invoke>>
  (batch_hard_delete_cascade) <<invoke>>
  (batch_restore_cascade) <<invoke>>
  (soft_remove_data) <<invoke>>
  (hard_remove_data) <<invoke>>
  (change_todo_visibility) <<invoke>>
  (transfer_todo_ownership) <<invoke>>
  (sync_data) <<invoke>>
  (search_data) <<invoke>>
  (export_to_cloud) <<invoke>>
  (statistics_get) <<invoke>>
}

package "navigate handlers" #LightGreen {
  (navigate_to_room) <<navigate>>
  (navigate_to_group) <<navigate>>
  (navigate_to_todo) <<navigate>>
  (open_settings) <<navigate>>
}

package "set handlers" #LightYellow {
  (update_todo_permissions) <<set>>
  (mark_message_read) <<set>>
  (mark_all_notifications_read) <<set>>
}

package "toast handlers" #LightPink {
  (show_toast) <<toast>>
  (show_error_toast) <<toast>>
}

package "modal handlers" #LightCoral {
  (show_confirm_delete) <<modal>>
  (show_share_dialog) <<modal>>
}

package "guard handlers" #GhostWhite {
  (auth_guard) <<guard>>
  (admin_guard) <<guard>>
  (owner_guard) <<guard>>
}

note bottom of invoke handlers
  Each #[kas_handler] replaces one #[tauri::command]
  Routing determined by handler type annotation
  invoke = async data operation returning Result<T, E>
end note

note bottom of guard handlers
  Guards run before handlers
  Return bool or redirect on failure
end note
@enduml
